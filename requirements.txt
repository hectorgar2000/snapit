# Forzar torch CPU-only (evita bajar CUDA ~2.5 GB, no hay GPU en el servidor)
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple/

ultralytics>=8.1.0   # YOLO-World requiere >= 8.1
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sqlmodel>=0.0.16
python-multipart>=0.0.9
pillow>=10.0.0
httpx>=0.27.0        # para los tests de API
PyJWT>=2.8.0         # JWT tokens
bcrypt>=4.0.0        # passlib sustituido por bcrypt directo (compat. Python 3.13)
psycopg2-binary>=2.9.0  # PostgreSQL (producción); SQLite no lo necesita pero no daña
